# M1 — Revalidate the ChatGPT Loop

## Goal

Verify and repair the existing extension against the current ChatGPT web UI before building new server complexity.

## Implementation note — 2026-08-22

The restart branch now contains the first revalidation hardening pass: composer/send selectors are scoped and ranked more conservatively, the overlay can export DOM/selector diagnostics, and tool calls keep an in-memory correlated state trace. These changes are implemented but runtime-sensitive boxes remain unchecked until verified in a real current ChatGPT session.

The first Windows acceptance run verified a clean `npm ci`, all current tests, and a successful patched `npm run build` for both extension and broker. The unpacked extension was then loaded in current Edge and the **Tool Shim · restart** overlay rendered successfully on `chatgpt.com`.

The first live conversation traffic verified that the current assistant-message selector detects real model output and that `hello`, `clock`, and `browser.tabs.list` execute end-to-end. With Auto Submit disabled, each result was inserted into the ChatGPT composer and could be manually submitted, verifying insert-only delivery. The large `browser.tabs.list` response also demonstrated that ordinary inline delivery remains functional for the current result size.

A later `hello` call was initially misclassified as a successful Auto Submit test because the result appeared in the conversation; the user clarified that they had manually pressed Send. Auto Submit therefore remains **unverified**. The implementation at that point called `button.click()` immediately after synthetic editor insertion and considered invoking the click itself to be success. It has now been changed to wait for an enabled submit control, click once, and verify that ChatGPT actually consumes/clears the composer before recording `SUBMITTED`. The trace records both send-control readiness latency and composer-clear latency.

That same test also exposed that identical autogenerated calls reused a content-derived call ID across page reloads (`call_52f675cf`). The parser was subsequently changed so autogenerated IDs combine a random per-content-script session nonce with a stable content hash: rescans in one page lifetime keep the same ID/fingerprint for dedupe, while the same call after a reload gets a distinct execution ID. Explicit model-provided IDs are preserved.

The restart HUD is now draggable by its title bar and persists its last viewport-constrained position across ChatGPT reloads so it can be moved away from page controls/content during testing.

## Checklist

- [x] Clean install dependencies.
- [x] Run protocol tests.
- [x] Build `dist/` successfully.
- [x] Load unpacked extension in current Edge.
- [x] Overlay appears on `chatgpt.com`.
- [x] Latest assistant message selector still works.
- [ ] Streaming output does not trigger incomplete calls.
- [ ] Fenced example calls remain ignored in a live page session.
- [ ] Fingerprint dedupe prevents duplicate execution under stressed DOM mutations.
- [x] `hello` end-to-end call succeeds.
- [x] `clock` end-to-end call succeeds.
- [ ] `clock.now` end-to-end call succeeds.
- [x] `browser.tabs.list` succeeds.
- [ ] `browser.tab.metadata` succeeds.
- [ ] `browser.tab.links` succeeds.
- [ ] `browser.tab.read_text` confirmation succeeds.
- [ ] blocked-domain behavior still works.
- [x] insert-only result mode works.
- [ ] auto-submit result mode works.
- [ ] send-button selector works across normal and streaming states.
- [ ] selector failures produce useful diagnostics.
- [ ] generate a debug bundle from a successful smoke test.
- [ ] generate a debug bundle from one intentionally failed call.

## Exit condition

A fresh browser session can execute the existing safe tool loop repeatedly without manual text copying and without duplicate calls.
